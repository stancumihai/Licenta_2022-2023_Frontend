import { mergeStyles } from '@fluentui/react';

export const customIconClassName: string = mergeStyles({
    position: 'relative',
    width: '1.1vw',
    margin: '0 1px',
    top: '85vh',
    marginRight: '0.5vw'
});

export const customIconClassNameSpecial: string = mergeStyles({
    position: 'relative',
    width: '1.3vw',
    margin: '0 1px',
    top: '85vh',
    marginRight: '0.5vw'
});